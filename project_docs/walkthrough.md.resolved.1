# AutoSEO Engine - MVP Walkthrough

## Status
- **Core Engine**: Running (NestJS + SQLite).
- **Crawler**: Active (Playwright).
- **Linking Engine**: Active (Regex Matcher).

## How to Run
1.  **Backend**: `npm run start:dev` (runs on port 3000).

## Features & Verification
### 1. Crawl a Site
```bash
node test-crawl.js
```
*Result*: Populates `articles` table with pages found on the target URL.

### 2. Manage Keywords
Add a keyword to match:
```bash
POST /keywords
{ "siteId": "...", "term": "NestJS", "url": "https://nestjs.com" }
```

### 3. Test Internal Linking
Check if the engine injects links correctly:
```bash
POST /keywords/test-link
{ "siteId": "...", "content": "I love NestJS framework." }
```
*Result*: `I love <a href="...">NestJS</a> framework.`

## Code Structure
- `src/modules/crawler`: Scrapes pages.
- `src/modules/intelligence`: Handles keywords & linking logic.
- `src/modules/sites`: Manages domain settings.
